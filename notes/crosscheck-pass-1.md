# Cross-check report

Outlines read: 40 of 40 lessons.

## 1. Lessons with no outline (0)

None.

## 2. Unknown lesson ids (0)

None.

## 3. Promises not acknowledged by the target (0)

A promises something to B, but B's *Picks up* doesn't name A. Either B should pick it up, or A should point elsewhere.

None.

## 4. Pick-ups not announced by the source (0)

B picks something up from A, but A's *Promises* doesn't name B. Usually A's promise list needs a line.

None.

## 5. Pick-ups from a lesson that isn't a prerequisite (0, plus 42 marked as E2 Recalls)

B relies on A, but A isn't among B's prerequisites (directly or through the chain), so a reader may not have seen it. Either add the prerequisite, or make it a brief "if you've done A" Recall (E2) that restates what B needs.

None.

## 6. Hooks marked unpaid (53)

- `ai-psychometrics`: Psychometrics of AI benchmarks → unpaid (problem 6).
- `ai-psychometrics`: Rater effects and many-facet models for human and machine raters → unpaid.
- `cdm`: Classification accuracy of attribute profiles → unpaid (Templin & Bradshaw, 2013, doi:10.1007/s00357-013-9129…
- `cdm`: Longitudinal CDMs (`hmcdm_spatialreasoning`) → unpaid.
- `cdm`: Mixture and latent-class IRT beyond CDMs → unpaid.
- `competitions`: Draws modelled properly (Davidson; ordinal models) → unpaid (a pointer to polytomous only).
- `competitions`: Glicko-2, TrueSkill and team-level ratings → unpaid (Going further).
- `competitions`: Intransitivity (a beats b beats c beats a) and multidimensional strength → unpaid.
- `competitions`: Raters who disagree: Friedman (2019)'s point is that Americans react to the same risks differently; judge-lev…
- `constructs`: Latent classes → `cdm`; mixture/latent-class item response models otherwise unpaid.
- `dif`: Fairness beyond items (the Standards' fairness chapter: access, accommodations, uses) → unpaid (landscape gap…
- `dimensionality`: Bifactor and testlet models: unpaid (`fa-confirmatory`'s DASS contrast raises them; a full treatment is beyon…
- `dimensionality`: Multidimensional adaptive testing → `item-banks-cat` (a paragraph there; unpaid otherwise).
- `equating`: Kernel equating, equating standard errors → unpaid (Going further).
- `equating`: Linking across languages and countries; alignment → unpaid (Haberman 2009 and the PIRLS contrast only; Asparo…
- `explanatory-irt`: How much the item residual matters to scores → unpaid.
- `fit-prediction`: Person fit and aberrant responding → unpaid (candidate for `irtrees`, rapid responding).
- `g-theory`: Occasions as a facet → `invariance-experience` (a Recall, per E2); ESM data otherwise unpaid.
- `g-theory`: Raters as a facet in item response models → unpaid (rater models, many-facet Rasch); flagged for the Beyond m…
- `guessing-priors`: Guessing as a property of respondents: the nominal model's "don't know" class → `nominal`; mixtures otherwise…
- `information`: Local dependence inflates information (items that share a passage): named here, unpaid in this chain. `dimens…
- `instrument-building`: Response styles (acquiescence, extreme responding) → `irtrees`; careless responding otherwise unpaid.
- `invariance-experience`: Item-level effects in polytomous outcomes → unpaid (the deep dive is dichotomous only, as in the vignette).
- `invariance-experience`: Life events with real IRW data → unpaid (no bereavement table in the IRW, 09-24; the spousal-loss result stay…
- `invariance-experience`: Response shift in health measurement (then-tests, reconceptualization) → unpaid.
- `irtrees`: Choosing among trees for the same categories → unpaid beyond AIC; an IMV candidate.
- `irtrees`: Whether response-style dimensions threaten validity arguments → unpaid (validity-evidence comes earlier in th…
- `item-banks-cat`: Item parameter drift over time (a pointer to `equating`'s anchors) → unpaid.
- `item-banks-cat`: Multistage testing → unpaid (a sentence in idea 3).
- `item-banks-cat`: Response time in adaptive tests → unpaid.
- `item-estimation`: Conditional ML → unpaid (one sentence here).
- `nominal`: Distractor-level DIF → unpaid (dif comes earlier; Suh & Bolt, 2011, is the pointer).
- `nominal`: Pattern scoring with the nominal model for reported scores, and the fairness of crediting wrong answers → unp…
- `nominal`: The ordinal–nominal continuum across the IRW as a deep dive → unpaid (a #4 candidate, citing Nalbandyan et al…
- `polytomous`: Does the choice among GRM, GPCM and sequential models ever matter across the IRW? Unpaid; a candidate IMV com…
- `response-time`: Rapid guessing as a latent class (a mixture) → unpaid.
- `response-time`: Using RT to flag aberrant behaviour (`credentialform_lnirt` flags 46 respondents) → unpaid; a problem (van de…
- `rt-process-models`: Diffusion IRT for real test items (minutes long, many options) → unpaid; the failure case says why it's hard.
- `rt-process-models`: Rapid guessing as a separate process (a guess–diffusion mixture) → unpaid.
- `rt-process-models`: The full diffusion model with trial-to-trial variability (ML per person, hierarchical Bayes) → unpaid (Going …
- `scale-properties`: A formal test of the cancellation conditions on IRW data → unpaid (a deep-dive candidate following Domingue, …
- `score-meaning`: Fairness of cut scores across groups (item-level DIF is `dif`'s; predictive bias `validity-evidence`'s; neith…
- `sem`: Longitudinal SEM (the twins data have waves) → unpaid.
- `sem`: Multigroup SEM (structural paths compared across groups; the measurement side, multigroup CFA invariance, is …
- `trials`: Learning across trials as change in θ → unpaid (dynamic IRT).
- `unfolding`: Ideal-point models for personality items (Stark et al., 2006) → unpaid; a problem only.
- `unfolding`: Multidimensional unfolding → unpaid.
- `unfolding`: Rankings and paired comparisons as unfolding data (`franco_2024_unfolding`) → unpaid; a problem only. The com…
- `validity-argument`: Consequences as evidence (Campbell's law; score reports that are never studied) → unpaid.
- `validity-causal`: Latent classes of rule users (the balance scale): a pointer to `cdm` (not a descendant, E2); mixture models o…
- `validity-causal`: Uses, once validity is only about the attribute: a separate process? → unpaid (left open; problem 6).
- `validity-evidence`: Consequences as evidence → unpaid.
- `validity-evidence`: Multitrait–multimethod as a CFA (trait and method factors) → unpaid.

## 7. Front to back: pick-ups from later in the first-course path (0)

B comes before A in the first-course path but picks something up from A. Reorder the path, or have B teach it and A recall it.

None.
